name: Build and Deploy to Portainer

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to howlil registry
        uses: docker/login-action@v3
        with:
          registry: registry.howlil.tech
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          build-args: |
            ASTRO_DB_REMOTE_URL=${{ secrets.ASTRO_DB_REMOTE_URL }}
            ASTRO_DB_APP_TOKEN=${{ secrets.ASTRO_DB_APP_TOKEN }}
          tags: |
            registry.howlil.tech/howlil/howlil-app:latest
            registry.howlil.tech/howlil/howlil-app:${{ github.sha }}

      - name: Notify Portainer GitOps
        run: |
          echo "✅ Image pushed to registry.howlil.tech. Portainer will auto-deploy via GitOps."
