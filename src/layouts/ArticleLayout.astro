---
import BaseLayout from "./BaseLayout.astro";
import { TableOfContents } from "../components/interactive";
import ArticleImagePreview from "../components/interactive/ArticleImagePreview";
import ArrowLink from "../components/content/ArrowLink.astro";
import { SITE } from "../config/site";
import { withBase } from "../lib/paths";

interface Props {
  title: string;
  description?: string;
  image?: string;
  articleTitle: string;
  date: string;
  backHref: string;
  backLabel: string;
  tags?: string[];
  tagLinkPrefix?: string;
  readingTime?: number;
  contentVariant?: 'editorial' | 'technical';
  recordLabel?: string;
}

const {
  title,
  description,
  image = '/og-image.svg',
  articleTitle,
  date,
  backHref,
  backLabel,
  tags = [],
  tagLinkPrefix = '',
  readingTime = 0,
  contentVariant = 'technical',
  recordLabel = 'Article',
} = Astro.props;

const siteUrl = Astro.site?.href || SITE.url;
const currentUrl = new URL(Astro.url.pathname, siteUrl).href;
const imageUrl = new URL(withBase(image), siteUrl).href;
const dateLabel = new Intl.DateTimeFormat('en', { month: 'short', year: 'numeric', timeZone: 'UTC' }).format(new Date(`${date}T00:00:00.000Z`));
---

<BaseLayout title={title} description={description} image={image}>
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Article",
    "headline": articleTitle,
    "datePublished": date,
    "author": { "@type": "Person", "name": SITE.name },
    "image": imageUrl,
    "mainEntityOfPage": currentUrl,
    ...(description ? { "description": description } : {})
  })} />

  <main id="main-content">
    <div class="site-shell">
      <header class="frame-section border-b border-gray-200 py-10 sm:py-12">
        <ArrowLink href={backHref} label={backLabel} direction="back" />
        <div class="mt-6 flex flex-wrap items-center gap-x-3 gap-y-2">
          <span class="eyebrow">{recordLabel}</span>
          <span class="eyebrow">{dateLabel}</span>
          {readingTime > 0 && <span class="eyebrow">{readingTime} min read</span>}
        </div>
        <h1 class="page-title mt-4 max-w-[22ch]">{articleTitle}</h1>
        <slot name="meta" />

        {tags && tags.length > 0 && !Astro.slots.has('meta') && (
          <div class="site-ui mt-5 flex flex-wrap items-center gap-x-3 gap-y-2 font-mono text-[10px] text-gray-500">
            {tags.map((tag: string, index: number) => (
              <Fragment>
                {index > 0 && <span aria-hidden="true">·</span>}
                <a href={`${tagLinkPrefix}?tag=${encodeURIComponent(tag)}`} class="hover:text-gray-900">{tag}</a>
              </Fragment>
            ))}
          </div>
        )}

        {Astro.slots.has('actions') && (
          <div class="site-ui mt-5 flex flex-wrap items-center gap-x-5 gap-y-2 text-[14px] text-gray-600"><slot name="actions" /></div>
        )}
      </header>

      <div class="grid grid-cols-1 lg:grid-cols-[minmax(0,1fr)_14rem]">
        <div class="min-w-0 px-[var(--frame-padding)] py-9 sm:py-10">
          <div class="reading-column">
            <slot name="cover" />
            <slot name="beforeContent" />
            <article class:list={["prose", { "prose-technical": contentVariant === 'technical' }]} id="article-content"><slot /></article>
          </div>
        </div>
        <aside class="hidden border-l border-gray-200 px-[var(--frame-padding)] py-7 lg:block">
          <div class="sticky top-6"><TableOfContents client:load /></div>
        </aside>
      </div>
    </div>
  </main>
  <ArticleImagePreview client:load />
</BaseLayout>

<style is:global>
  .prose {
    color: var(--color-text-body);
    max-width: 100%;
    font-family: var(--font-prose);
    text-align: left;
    font-size: var(--text-body-size);
    line-height: var(--text-body-line-height);
  }
  .prose-technical { line-height:var(--text-body-line-height); }
  .prose h1 { display:none; }
  .prose h2 {
    font-size:1.5rem;
    font-weight:600;
    font-family:var(--font-heading);
    line-height:1.25;
    margin-top:3.25rem;
    margin-bottom:1rem;
    color:var(--color-text-heading);
    scroll-margin-top:2rem;
    letter-spacing:-.03em;
  }
  .prose h2:first-child { margin-top:0; }
  .prose h3 {
    font-size:1.125rem;
    font-weight:600;
    font-family:var(--font-heading);
    margin-top:2.25rem;
    margin-bottom:.75rem;
    color:var(--color-text-heading);
    scroll-margin-top:2rem;
    letter-spacing:-.015em;
  }
  .prose h4 { font-family:var(--font-heading); color:var(--color-text-heading); }
  .prose p { margin-bottom:1.15rem; line-height:var(--text-body-line-height); color:var(--color-text-body); font-family:var(--font-prose); }
  .prose ul,.prose ol { margin-bottom:1.25rem; padding-left:1.35rem; }
  .prose ul { list-style-type:disc; }
  .prose ol { list-style-type:decimal; }
  .prose li { margin-bottom:.5rem; line-height:1.65; color:var(--color-text-body); font-family:var(--font-prose); display:list-item; }
  .prose strong { font-weight:600; color:var(--color-text-heading); }
  .prose blockquote {
    margin:1.75rem 0;
    border-left:2px solid var(--color-accent);
    padding:.2rem 0 .2rem 1rem;
    color:var(--color-text-secondary);
  }
  .prose blockquote p:last-child { margin-bottom:0; }
  .prose code {
    color:var(--color-text-heading);
    padding:.1rem .28rem;
    border-radius:.25rem;
    background:var(--color-surface-muted);
    font-size:.8125rem;
    font-family:var(--font-code);
  }
  .prose pre {
    background:#171816;
    color:#f1f1ed;
    padding:1rem 1.05rem;
    border-radius:.375rem;
    overflow-x:auto;
    margin:1.5rem 0 1.75rem;
    border:1px solid var(--color-border-strong);
  }
  .prose pre code { background:transparent; padding:0; color:#f1f1ed; }
  .prose hr { border-top:1px solid var(--color-border); margin:2.5rem 0; }
  .prose table {
    width:100%;
    min-width:36rem;
    border-collapse:collapse;
    margin:1.5rem 0 1.75rem;
    table-layout:auto;
    font-size:.875rem;
    border-top:1px solid var(--color-border-strong);
    border-bottom:1px solid var(--color-border-strong);
  }
  .prose th,.prose td { border-bottom:1px solid var(--color-border); padding:.75rem .7rem; text-align:left; vertical-align:top; word-wrap:break-word; overflow-wrap:break-word; }
  .prose tr:last-child td { border-bottom:0; }
  .prose th { background:transparent; font-weight:600; font-family:var(--font-heading); color:var(--color-text-heading); }
  .prose td { color:var(--color-text-body); font-family:var(--font-prose); }
  .prose a { color:var(--color-link); text-decoration:underline; text-underline-offset:2px; }
  .prose a:hover { color:var(--color-link-hover); }
  .prose img { cursor:zoom-in; border-radius:.375rem; border:1px solid var(--color-border); transition:opacity .2s ease; }
  .prose img:hover { opacity:.94; }
  .prose.prose-technical,.prose.prose-technical p,.prose.prose-technical li,.prose.prose-technical td { font-family:var(--font-heading); }
  .prose .table-wrapper { overflow-x:auto; }

  @media (max-width: 767px) {
    .prose table { display:block; overflow-x:auto; white-space:normal; -webkit-overflow-scrolling:touch; }
  }
</style>
